#!/bin/bash


run_name=$1
cd $SCRIPTS
cd ..

python scripts/package_summaries.py /clir /outputs/ \
    /outputs/packages/$run_name scriptssumop1 $run_name

cd  /outputs/packages/$run_name
tar -zcf /outputs/packages/$run_name.tar.gz query*
