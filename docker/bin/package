#!/bin/bash


run_name=$1
cd $SCRIPTS
cd ..

python scripts/package_summaries.py /CLIR /outputs \
    /outputs/packages/$run_name scriptssum1 $run_name

cd  /outputs/packages/$run_name
tar -zvf /outputs/packages/$run_name.tar.gz query*
